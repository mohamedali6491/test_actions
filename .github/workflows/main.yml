steps:
  - name: Checkout code
    uses: actions/checkout@v4 # Updated to v4 for the latest node support

  - name: Setup Go
    uses: actions/setup-go@v5 # Updated to v5 (supports caching automatically)
    with:
      go-version: '1.14.0'

  - name: Run Hello World
    run: go run helloworld.go
